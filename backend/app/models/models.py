"""Supplemental models — ep-backend specific models not in app.db.models.

IMPORTANT: Most models (User, Subject, KnowledgePoint, Question, WrongAnswer,
ChatSession, etc.) are in app.db.models (ep-db owned). Import from there.
Only add models here that ep-db does NOT cover and that are ep-backend owned.
"""
