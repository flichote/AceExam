# AceExam M3.5 任务图启动手册（kanban 多角色协作）

> **适用场景**：M3（体验增强 + 增长功能）已交付后，一键启动 M3.5 开发（M3 剩余功能：TTS 语音讲解 / UGC 题库共建 / 成绩单海报分享 / 班级排行榜）。
> **前置**：6 个 ep-* 角色就绪，主 profile 网关在跑；board `aceexam` 已建；T19 已产出契约文档。
> **M3.5 目标**（PRD §3）：语音讲解（TTS，第二层）+ 班级排行榜 / 成绩单海报分享 / UGC 题库共建（第三层）。挂科预警已随 M3 交付，M3.5 补齐剩余增长与体验功能。
> **契约来源**：模块设计见 `docs/architecture.md` §12（含决策锁定表 D9~D14，修订 D3）；API 契约见 `docs/api.md` §12（新增 8 端点 + 修订 1，总计 43）；表结构增量见 `docs/database.md` §10（T20 产出）。

## 任务图设计

```
T19 ep-arch（架构师）                 ← root，本任务已交付契约
├── T20 ep-backend（后端）            ← parent T19：M3.5 API（TTS/UGC/班级/海报 + 小表迁移 0004）
│     └── T21 ep-ai（AI 工程师）      ← parent T19：tts_service + ugc_service（与 T20 并行，接口先行）
├── T22 ep-frontend（前端）           ← parent T19：语音播放/投稿入口/班级榜/海报 canvas
└── T23 ep-qa（测试）                 ← parents T20+T21+T22：M3.5 验收测试
```

**已建卡片 ID（2026-08-08，T19 任务图中捕获）**：

| 任务 | 卡片 ID | 备注 |
|---|---|---|
| T19 | t_efbbd5ad | 本任务（架构增量 + API 契约），done 后解锁 T20/T21/T22 |
| T20 | t_3e4e28db | 待 T19 done 自动解锁 |
| T21 | t_c83836d0 | 待 T19 done 自动解锁 |
| T22 | t_aa9dabe8 | 待 T19 done 自动解锁 |
| T23 | t_7b665a57 | parents=T20+T21+T22（依赖边已补全），全部 done 后解锁 |

**并行规则**（沿用 M1/M2/M3 验证结论）：

- T20/T21/T22 三个不同 profile → 可并行跑（各写各的目录：T20 写 api/schemas/alembic，T21 写 services/tests，T22 写 frontend）
- T20 → T21 服务边界：T20 路由只 import 调用 `tts_service.py` / `ugc_service.py`（T21 交付物）；若 T21 未交付先返回 501 或 mock 兜底（接口先行，architecture.md §12.6），T21 落地后替换并删除内联——禁止双实现长期并存
- T23 等 T20 + T21 + T22 → dispatcher 自动解锁
- 同一 profile 绝不并行两个任务（会互相踩 git）

## 依赖说明

| 边 | 含义 | 满足方式 |
|---|---|---|
| T19 → T20/T21/T22 | 契约先行：`docs/architecture.md` §12（模块设计 + 决策锁定 D9~D14 + 表增量约定）+ `docs/api.md` §12（字段级契约） | 各任务开工前先读 architecture.md §12 与 api.md §12；T20 按 §12.5 建表（0004 迁移）+ 按 api.md §12 实现路由，T21 按 §12.1/§12.2 实现服务，T22 按 §12 对接页面 |
| T20 → T21（服务边界） | `tts_service.py`/`ugc_service.py` 归 T21；T20 路由只 import 调用 | T20 若等不及 T21，先返回 501/mock 兜底（接口先行），T21 落地后替换并删除内联（卡片 comment 注明） |
| T20 + T21 + T22 → T23 | 端到端验收需要前后端 + AI 服务联调 | dispatcher 自动解锁（依赖边已补全） |

> ⚠️ **T21 隐含依赖**：TTS 测试必须 mock edge-tts（不真调微软端点）；无网络时 `tts_service` 返回 501/明确错误（契约 502 由 T20 路由层映射）。
> ⚠️ **T20 卡片草案修订**：T20 body 中"班级用 class_name 简单方案"被架构定案取代——采用 classes 表 + 邀请码模型（architecture.md §12.4 决策 D10）；`POST /me/class` 的 `{name}` 分支即建班、`{invite_code}` 分支即加入，api.md §12.6 为准。

## 文件边界（避免重名冲突）

| 角色 | 写入目录 | 关键文件 |
|---|---|---|
| T20 ep-backend | `backend/app/api/`、`backend/app/schemas/`、`backend/alembic/`、`docs/database.md` | `api/v1/{chat,questions,admin,me,leaderboard}.py` 增量；`schemas/{tts,ugc,classroom,share_card}.py`；`alembic/versions/0004_m35_*.py`（classes 新表 + users.class_id + questions 扩展，architecture.md §12.5）；database.md §10 增量 |
| T21 ep-ai | `backend/app/services/` + `backend/tests/test_ai_m35.py` | `tts_service.py`（edge-tts 封装 + 文本清洗 + 磁盘缓存）、`ugc_service.py`（预检规则 + 可选自动审核）；复用 `llm_gateway.py` / `ocr_service.py`（import 方式，别改它们） |
| T22 ep-frontend | `frontend/` | 语音播放（`uni.createInnerAudioContext()`）、UGC 投稿入口、班级页 + 排行榜 scope 切换、海报 canvas（type=2d / H5 canvas）；mock 保留在 `frontend/src/mock/` 做 fallback |
| T23 ep-qa | `backend/tests/`（除 test_ai_* 外）+ `docs/qa/` | M3.5 验收测试 + test-report.md 四里程碑汇总；只加测试不改业务代码 |

## 执行步骤（个人电脑，git-bash）

### 1. 创建/补全任务（ID 立即捕获，别手敲）

```bash
hermes kanban boards switch aceexam

# T20/T21/T22 已建（parent T19）；若缺失按 M2 模式补建：
# hermes kanban create "T20 M3.5 API" --profile ep-backend --parent "$T19" --body "$(cat kanban/boards/aceexam/t20-body.md)"
# hermes kanban create "T21 TTS+UGC解析服务" --profile ep-ai --parent "$T19" --body "$(cat kanban/boards/aceexam/t21-body.md)"
# hermes kanban create "T22 M3.5页面" --profile ep-frontend --parent "$T19" --body "$(cat kanban/boards/aceexam/t22-body.md)"

# T23（parents T20+T21+T22）：
# hermes kanban create "T23 M3.5验收测试" --profile ep-qa --parent "$T20" --parent "$T21" --parent "$T22" --body "$(cat kanban/boards/aceexam/t23-body.md)"
```

> 当前板上 T20（t_3e4e28db）、T21（t_c83836d0）、T22（t_aa9dabe8）、T23（t_7b665a57）已存在；T23 的 parents 已含 T20/T21/T22（依赖边已补全）。

### 2. 监控（主控主动汇报，别等）

```bash
hermes kanban list           # 状态总览
hermes kanban log <t_id>     # 看 worker 实时在干什么
git log --oneline -5         # 新提交
```

**进度汇报节奏**：每 10~15 分钟给用户一个快照（✓ done / ● running / ⊘ blocked / ◻ todo + 各 worker 实际动作 + 新 commit）。

### 3. worker 异常恢复速查（沿用 M1/M2/M3）

| 症状 | 处理 |
|---|---|
| 卡 `running` 但无日志 | 网关停了 → `hermes gateway status` + 重启，等 ~70s |
| 日志显示 API 连接错误（秒级失败） | 网络抖动 → `hermes kanban unblock <id> --reason "transient"` 重派一次 |
| 日志显示长上下文超时 | 别重派，主控收尾（M2-taskgraph §4 模式） |
| 迭代预算耗尽 | 主控收尾：log 看进度 → 本地验证 → 提交推送 → complete |

### 4. 主控收尾模式（worker 干不完时）

```bash
hermes kanban log <t_id> | tail -30
env -u PYTHONPATH -u VIRTUAL_ENV backend/.venv/Scripts/python.exe -m pytest -v
cd <repo> && git add <对应目录> && git commit -m "主控收尾: ..." && git push
hermes kanban comment <t_id> "✅ 收尾：<证据>"
hermes kanban complete <t_id>
```

## 验收标准（M3.5 完成 = 全部满足）

- [ ] `docs/architecture.md` §12：TTS / UGC / 海报 / 班级四模块设计 + 决策锁定表（D9~D14，修订 D3）
- [ ] `docs/api.md` §12：8 个新端点字段级契约 + `GET /leaderboard` 修订（scope=class），端点总数 35 → 43
- [ ] Alembic 0004 迁移：`classes` 新表 + `users.class_id` + `questions` 扩展（status pending/rejected + submitted_by/reviewed_by/reviewed_at/reject_reason）；TTS/分享卡确认无新表（architecture.md §12.5 决策）
- [ ] TTS：`POST /chat/explain/{session_id}/tts`（文本清洗 + 磁盘缓存 + voice 白名单；会员边界 403）+ `GET /tts/audio/{hash}.mp3`（FileResponse 音频流，404 缓存未命中）
- [ ] UGC：`POST /questions/ugc`（规则预检 + source=ugc + status=pending）+ `GET /admin/questions/ugc`（admin 鉴权 + status 过滤）+ `POST /admin/questions/{id}/review`（approve→active / reject→rejected 带理由；已审核 409）
- [ ] 班级：`POST /me/class`（建班/加入幂等 + invite_code 唯一）+ `GET /me/class`（我的班级 + my_rank）+ `GET /leaderboard?scope=class`（成员过滤 + 未加入 422 CLASS_NOT_JOINED）
- [ ] 海报：`GET /me/share-card`（连胜/掌握度/做题量/近 7 天正确率/班级/考试聚合，口径与 dashboard 一致）
- [ ] 前端四功能点 + 真实 API 对接（TTS 播放、UGC 投稿入口、班级榜、海报 canvas 导出保存），`npm run build` 通过（h5 + mp-weixin）
- [ ] pytest 全绿（含 M3.5 验收测试 + M1~M3 回归），`docs/qa/test-report.md` 更新四里程碑汇总
- [ ] 全部代码已 push，`git log --oneline` 每个任务一条以上 feature commit
